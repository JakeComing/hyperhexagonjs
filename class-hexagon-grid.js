class hexagonGrid {
  constructor(gridHeight, gridWidth, hexagonDimensions) {
    this.gridHeight = gridHeight;
    this.gridWidth = gridWidth;
    this.hexagonDimensions = hexagonDimensions;
  }
}
